<%= form_for([@post, @post.suggestions.build]) do |f| %>
  <%= f.text_field :content, class: 'form-control' %>
  <%= f.hidden_field :user_id, :value => current_user.id %><br>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
